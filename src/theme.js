import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#b75d69',
    },
    secondary: {
      main: '#7678ed',
    },
    background: {
      default: '#efe6dd',
    },
  },
});

export default theme;
