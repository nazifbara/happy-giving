import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#b75d69',
    },
    secondary: {
      main: '#7678ed',
    },
    background: {
      default: '#f2f2f2',
    },
  },
});

export default theme;
