import { default as CheckoutView } from './CheckoutView';
import { default as ProjectView } from './ProjectView';
import { default as HomeView } from './HomeView';

const routes = [ProjectView, HomeView, CheckoutView];

export default routes;
